:plain
  $("li").on("click", function() {
    console.log(1);
    $("li.active").removeClass("active");
    $(this).addClass("active");
  });
  $('.container-proto-list .row').html('#{ escape_javascript(render("/prototypes/prototype", prototypes: @prototypes ))}');
